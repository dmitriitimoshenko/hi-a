apiVersion: 1

providers:
  - name: mail-mapper
    orgId: 1
    folder: Mail Mapper
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
